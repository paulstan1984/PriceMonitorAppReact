import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.paul.pricemonitorapp',
  appName: 'PriceMonitorAppReact',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
